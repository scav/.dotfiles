set -g status-interval 1
set -g default-terminal "screen-256color"

set-option -g status-position top

### --- Tabs / Status Bar --- ###
set -g status on
setw -g monitor-activity on

set -g mouse on

# leader
set -g prefix C-b
unbind C-b
bind C-b send-prefix

bind t new-window

# Close pane with confirmation
bind x confirm-before -p "kill-pane? (y/n)" kill-pane

# split
bind - split-window -v
bind = split-window -h

# navigate panes 
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

# resize panes 
bind -n M-h resize-pane -L 1
bind -n M-j resize-pane -D 1
bind -n M-k resize-pane -U 1
bind -n M-l resize-pane -R 1
bind -n M-H resize-pane -L 10
bind -n M-J resize-pane -D 10
bind -n M-K resize-pane -U 10
bind -n M-L resize-pane -R 10

### --- Right Status (Hostname + Session Name) --- ###
set -g status-right " #S | #(hostname) "

# switch or create session using sesh
bind-key "s" run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:55%' \
    --preview 'sesh preview {}'
)\""

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'fabioluciano/tmux-tokyo-night'

# configure theme
set -g @theme_plugin_weather_location 'Bergen, Norway'
set -g @theme_plugin_weather_format '%w%t%c+H:%h'
set -g @theme_plugins 'datetime,weather,battery'
set -g @theme_transparent_status_bar 'true'


run '~/.tmux/plugins/tpm/tpm'
